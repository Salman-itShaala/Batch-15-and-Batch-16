const marks = [67, 87, 44, 59, 98, 71, 100, 88];

// functions

// pop --> will delete last element and returns it
// push --> we can add element at the end and it will return new length
// shift --> we can delete first element
// unshift --> we can add element at first position
// splice --> we can add / delete elements wherever we want

// marks[0] --> marks.at(0)
// marks[1] --> marks.at(1)
// marks[2] --> marks.at(2)

// at --> used to retrive elements
// includes --> to search elements will return Boolean
// join --> Adds all the elements of an array into a string, separated by the specified separator string.
// slice --> Returns a copy of a section of an array.

// 

console.log(marks)

console.log(marks.reverse());

const names = ["a", "z", "s", "c", "f", "b"];

console.log(names.sort());

console.log(marks.sort());